# ============================================================================
# DO NOT EDIT THIS FILE DIRECTLY.
# This workflow is managed by Terraform and rendered from:
#   TailorBrands/github-resources -> terraform/repos/ember-gtm/workflows/workflows/
# Any change made directly in this repo will be reverted on the next apply.
# ============================================================================

name: Pull request CI

on:
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
    - name: Use Node.js
      uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
      with:
        node-version: "22"
        cache: "yarn"
    - run: yarn install --frozen-lockfile
    - run: yarn run test
